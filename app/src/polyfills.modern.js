//These polyfills get included into the production modern bundle.
//Note, if webpack.config.js babel-loader config, useBuiltins is set to: 'usage', then leave the next import commented out:
// import "@babel/polyfill";
